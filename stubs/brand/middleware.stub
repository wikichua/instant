<?php

namespace Brand\{%brand_name%}\Middlewares;

use Closure;
use Illuminate\Http\Request;

class {%brand_name%}Middleware
{
    public function handle(Request $request, Closure $next)
    {
        return $next($request);
    }
}
